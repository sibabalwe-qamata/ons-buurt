{
  "name": "ons-buurt",
  "private": true,
  "scripts": {
    "dev": "concurrently -n fe,be \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run start:dev --prefix backend"
  },
  "devDependencies": {
    "@types/node": "^22.16.5",
    "@vitejs/plugin-react-swc": "^3.11.0",
    "concurrently": "^9.0.0",
    "vite": "^5.4.19",
    "vitest": "^3.2.4"
  }
}
